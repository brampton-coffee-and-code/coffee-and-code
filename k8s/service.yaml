apiVersion: v1
kind: Service
metadata:
  labels:
    app: coffee-and-code
  name: coffee-and-code
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
  selector:
    app: coffee-and-code
  type: ClusterIP
